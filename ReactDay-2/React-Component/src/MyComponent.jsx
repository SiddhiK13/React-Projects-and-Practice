// 1. Create a React application to create and use a component in React.
// 2. Create a React application to install and use Bootstrap in React using CLI.

import React from "react";

function MyComponent() {
    return (
        <div>
            <h2 className="heading text-success bg-warning">This is my first React Component!</h2>
            <p>Components make React modular and reusable.</p>
        </div>
    );
}

export default MyComponent;
